<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackConposeCourseProfileDog"
        tools:targetApi="31">
        <activity
            android:name=".course3.BasicsCodelab"
            android:exported="true"
            android:label="@string/title_activity_basics_codelab"
            android:theme="@style/Theme.JetpackConposeCourseProfileDog" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".course2.RowsColumnsMainActivity"
            android:exported="true"
            android:label="@string/title_activity_rows_columns_main"
            android:theme="@style/Theme.JetpackConposeCourseProfileDog">

        </activity>
        <activity
            android:name=".course.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetpackConposeCourseProfileDog"></activity>
    </application>

</manifest>